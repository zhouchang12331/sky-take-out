<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.ShoppingCartMapper">
    <insert id="insertBatch">
        insert into shopping_cart (user_id, dish_id, setmeal_id, name, image, amount, number, create_time)
        values
        <foreach collection="shoppingCartList" item="cart" separator=",">
            (#{cart.userId}, #{cart.dishId}, #{cart.setmealId}, #{cart.name}, #{cart.image}, #{cart.amount}, #{cart.number}, #{cart.createTime})
        </foreach>
    </insert>

    <select id="list" resultType="com.sky.entity.ShoppingCart">
        select * from shopping_cart
        <where>
            <if test="userId!=null">
                and user_Id=#{userId}
            </if>
            <if test="dishId!=null">
                and dish_Id=#{dishId}
            </if>
            <if test="setmealId!=null">
                and setmeal_Id=#{setmealId}
            </if>
            <if test="dishFlavor !=null">
                and dish_Flavor=#{dishFlavor}
            </if>

        </where>
    </select>
</mapper>